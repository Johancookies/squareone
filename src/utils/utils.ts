import { OptionsObject } from "notistack";

export const successToast: OptionsObject = {
  variant: "success",
  autoHideDuration: 2500,
};

export const errorToast: OptionsObject = {
  variant: "error",
  autoHideDuration: 2500,
};

export const infoToast: OptionsObject = {
  variant: "info",
  autoHideDuration: 2500,
};
